name: Helm chart Validation

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - review_requested
      - labeled

# Set env
env:
  # If the name of the branch is main, NODE_ENV is stage, otherwise it is dev .
  APP_NAME: bug
  NODE_ENV: ${{ github.ref == 'refs/heads/main' && 'stage' || 'dev' }}
  AWS_REGION: ap-northeast-2
  ECS_TASK_DEFINITION: task-definition.json

jobs:
  get-image-tag:
    name: get-tag
    runs-on: ubuntu-latest
    if: contains( github.event.pull_request.labels.*.name, ${{env.APP_NAME}} ) || github.ref == 'refs/heads/main'
    steps:
      - name: test1
        run: |
          echo ${{ github.event.pull_request.labels.*.name }}
          echo ${{ env.APP_NAME }}
      - name: test
        if: contains( github.event.pull_request.labels.*.name, env.APP_NAME ) || github.ref == 'refs/heads/main'
        run: |
          echo ${{ env.NODE_ENV }}
          echo ${{ github.ref }}
          echo ${{ env.AWS_REGION }}
      - name: test3
        if: contains( github.event.pull_request.labels.*.name, 'test' ) || github.ref == 'refs/heads/main'
        run: |
          echo ${{ env.NODE_ENV }}
          echo ${{ github.ref }}
          echo ${{ env.AWS_REGION }}